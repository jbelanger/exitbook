{
  "name": "@crypto/shared-utils",
  "license": "AGPL-3.0",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utilities for cryptocurrency platform",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "postinstall": "pnpm list --depth=0 || echo '⚠️  Warning: Missing peer dependencies. Please install them manually.'",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "lint": "eslint \"src/**/*.ts*\"",
    "typecheck": "tsc --noEmit",
    "test": "jest --detectOpenHandles --forceExit --passWithNoTests",
    "tsc": "tsc --project tsconfig.json",
    "clean": "rm -rf node_modules"
  },
  "dependencies": {
    "decimal.js": "^10.6.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.0",
    "tsconfig": "workspace:*",
    "tsx": "^4.20.3",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "@crypto/core": "workspace:*",
    "@crypto/infrastructure": "workspace:*",
    "@crypto/shared-logger": "workspace:*"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}