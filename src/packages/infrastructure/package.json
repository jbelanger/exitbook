{
  "name": "@crypto/infrastructure",
  "license": "AGPL-3.0",
  "version": "0.1.0",
  "private": true,
  "description": "Infrastructure services for cryptocurrency platform",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "postinstall": "pnpm list --depth=0 || echo '⚠️  Warning: Missing peer dependencies. Please install them manually.'",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "lint": "eslint \"src/**/*.ts*\"",
    "typecheck": "tsc --noEmit",
    "test": "jest --detectOpenHandles --forceExit --passWithNoTests",
    "tsc": "tsc --project tsconfig.json",
    "clean": "rm -rf node_modules"
  },
  "dependencies": {
    "sqlite3": "^5.1.7",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.1",
    "tsconfig": "workspace:*",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  },
  "peerDependencies": {
    "@crypto/core": "workspace:*",
    "@crypto/shared-logger": "workspace:*",
    "@crypto/shared-utils": "workspace:*"
  }
}