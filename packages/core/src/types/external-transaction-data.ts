/**
 * Types exported from schemas for single source of truth
 * For runtime validation, import the schemas from schemas/external-transaction-data.ts
 */
export type { ExternalTransaction, ExternalTransactionData } from '../schemas/external-transaction-data.ts';
