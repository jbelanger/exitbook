/**
 * Types exported from schemas for single source of truth
 * For runtime validation, import the schemas from schemas/token-metadata.ts
 */
export type { TokenMetadata } from '../schemas/token-metadata.ts';
