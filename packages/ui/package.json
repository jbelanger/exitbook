{
  "name": "@exitbook/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Event-driven progress system for CLI and web UI",
  "license": "AGPL-3.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf node_modules dist",
    "lint": "eslint \"src/**/*.ts\"",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "1.0.0-alpha.6"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsconfig": "workspace:*",
    "tsx": "^4.20.4",
    "typescript": "^5.9.3"
  }
}
