/**
 * Large feature packages use curated capability subpaths rather than a broad root barrel.
 *
 * Use one of:
 * - `@exitbook/ingestion/adapters`
 * - `@exitbook/ingestion/asset-review`
 * - `@exitbook/ingestion/balance`
 * - `@exitbook/ingestion/events`
 * - `@exitbook/ingestion/import`
 * - `@exitbook/ingestion/process`
 * - `@exitbook/ingestion/ports`
 */
export {};
