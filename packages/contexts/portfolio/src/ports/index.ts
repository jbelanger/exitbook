// Effect "ports" (service tags/interfaces) the app/core depend on.
// Implementations live under adapters/.
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- Placeholder interface to be filled in later
export interface RepositoryPort {
  /* define methods */
}
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- Placeholder interface to be filled in later
export interface MessageBusPort {
  /* define methods */
}
// export const RepositoryTag = Symbol.for("RepositoryPort") as unique symbol;
