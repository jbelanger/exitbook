{
  "name": "@exitbook/sqlite",
  "version": "0.1.0",
  "private": true,
  "description": "Shared SQLite + Kysely database utilities",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf node_modules dist",
    "lint": "eslint \"src/**/*.ts\"",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "test": "vitest run"
  },
  "dependencies": {
    "@exitbook/foundation": "workspace:*",
    "@exitbook/logger": "workspace:*",
    "better-sqlite3": "^12.8.0",
    "kysely": "^0.28.12"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "tsconfig": "workspace:*",
    "tsx": "^4.21.0"
  }
}
