/**
 * TypeScript types for NEAR blockchain provider
 * All types are inferred from Zod schemas for type safety
 */

export type { NearAccountChange, NearAction, NearTokenTransfer, NearTransaction } from './schemas.js';
