/**
 * TypeScript types for NEAR blockchain provider - V2 (receipt-based model)
 * All types are inferred from Zod schemas for type safety
 */

export type {
  NearAction,
  NearBalanceChange,
  NearReceipt,
  NearReceiptEvent,
  NearReceiptOutcome,
  NearTokenTransfer,
  NearTransaction,
} from './schemas.v2.js';
