{
  "name": "@exitbook/logger",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf node_modules dist",
    "lint": "eslint \"src/**/*.ts\"",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "test": "vitest run"
  },
  "dependencies": {
    "pino": "^10.3.0",
    "pino-pretty": "^13.1.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "tsconfig": "workspace:*"
  }
}
