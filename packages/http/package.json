{
  "name": "@exitbook/http",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP client utilities with rate limiting and resilience patterns",
  "license": "AGPL-3.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf node_modules dist",
    "lint": "eslint \"src/**/*.ts\"",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "test": "vitest run"
  },
  "dependencies": {
    "@exitbook/logger": "workspace:*",
    "undici": "^7.6.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsconfig": "workspace:*"
  }
}
