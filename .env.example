# Application Configuration
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/exitbook"
DATABASE_POOL_SIZE=10
DATABASE_SSL_MODE=prefer

# Path to the JSON file containing complex provider configuration
# This allows for easy overrides in different environments (e.g., staging.providers.json)
PROVIDERS_CONFIG_PATH=./config/providers.config.json